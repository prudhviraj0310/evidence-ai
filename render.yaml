services:
  - type: web
    name: evidence-ai
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm run render-build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: EVIDENCE_MODE
        value: auto
      - key: GEMINI_API_KEY
        sync: false
